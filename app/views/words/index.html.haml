%h1 Words
%table
  %thead
    %tr
      %th Word
  %tbody
    - @words.each do |word|
      %tr
        %td= word.wordform
        %td= link_to "Show", word
        %td= link_to "Edit", edit_post_path(word)
        %td= link_to "Destroy", word, method: :delete, data: { confirm: "Are you sure?" }
%br/
= link_to "New Word", new_word_path
